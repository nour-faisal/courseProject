import { CutomeCorPipe } from './cutome-cor.pipe';

describe('CutomeCorPipe', () => {
  it('create an instance', () => {
    const pipe = new CutomeCorPipe();
    expect(pipe).toBeTruthy();
  });
});
